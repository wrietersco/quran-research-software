# Optional: CAMeL Tools (MSA morphology + MLE disambiguation + morphological tokenizer).
# Use a Python version supported by wheels (typically 3.8–3.12; check PyPI for 3.13).
#
#   pip install -r requirements-camel.txt
#   camel_data -i light
#
# Then:
#   python scripts/camel_morphology_demo.py
#   python scripts/run_morphology_pipeline.py --token "كلمة" -v
# Ensemble + optional LLM rerank:
#   python scripts/run_morphology_pipeline.py --ensemble --sentence "..." --llm-rerank
#   (requires OPENAI_API_KEY and MORPH_LLM_MODEL optional)
#
camel-tools>=1.5.5,<1.6
